name: bitbucket
unique_key: bitbucket

description: Bitbucket is a Git-based source code repository hosting service owned by Atlassian.
logo: https://raw.githubusercontent.com/SamparkAI/open-logos/c776dbf1cd2b4f79cab0a1feafe5045b92e12ca9/bitbucket.svg
categories: [dev-tools]

auth_schemes:
  - scheme_name: OAuth2
    auth_mode: OAUTH2
    authorization_url: "https://bitbucket.org/site/oauth2/authorize"
    token_url: "https://bitbucket.org/site/oauth2/access_token"

    token_params:
      grant_type: authorization_code
    authorization_params:
      response_type: code
    proxy:
      base_url: "{{base_url}}"
      #state removed
    fields:
      - name: base_url
        displayName: Base URL
        description: "The base URL for the Bitbucket API."
        type: string
        default: "https://api.bitbucket.org"
